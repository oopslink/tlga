# 🎨 统一通知系统使用指南

## 📦 系统组件

### 1. Modal 对话框
用于需要用户确认或查看重要信息的场景。

### 2. Toast 通知
用于短暂的提示信息，自动消失。

---

## 🔔 Modal 使用方法

### 导入

```typescript
import { useModal } from '@/composables/useModal'

const { showConfirm, showSuccess, showError, showWarning, showInfo } = useModal()
```

### API

#### 1. 确认对话框

```typescript
const confirmed = await showConfirm('确定要删除这条记录吗？', '删除确认')
if (confirmed) {
  // 用户点击了"确定"
} else {
  // 用户点击了"取消"
}
```

#### 2. 成功提示

```typescript
await showSuccess('保存成功！', '操作成功')
```

#### 3. 错误提示

```typescript
await showError('保存失败，请重试', '操作失败')
```

#### 4. 警告提示

```typescript
await showWarning('您的账号即将过期', '注意')
```

#### 5. 信息提示

```typescript
await showInfo('系统将在5分钟后维护', '系统通知')
```

### 完整示例

```vue
<script setup lang="ts">
import { useModal } from '@/composables/useModal'

const { showConfirm, showSuccess, showError } = useModal()

async function deleteTask() {
  const confirmed = await showConfirm(
    '删除后无法恢复，确定要删除吗？',
    '删除任务'
  )

  if (confirmed) {
    try {
      // 执行删除操作
      await api.deleteTask(taskId)
      await showSuccess('任务已删除', '删除成功')
    } catch (error) {
      await showError('删除失败，请重试', '操作失败')
    }
  }
}
</script>
```

---

## 🍞 Toast 使用方法

### 导入

```typescript
import { useToast } from '@/composables/useToast'

const { success, error, warning, info } = useToast()
```

### API

#### 1. 成功提示

```typescript
success('保存成功！')
// 自定义持续时间（毫秒）
success('保存成功！', 5000)
```

#### 2. 错误提示

```typescript
error('网络连接失败')
error('上传失败，文件过大', 5000)
```

#### 3. 警告提示

```typescript
warning('请先填写必填项')
warning('磁盘空间不足', 4000)
```

#### 4. 信息提示

```typescript
info('数据正在同步中...')
info('新版本已发布', 3000)
```

### 完整示例

```vue
<script setup lang="ts">
import { useToast } from '@/composables/useToast'

const { success, error, warning } = useToast()

async function saveData() {
  // 开始保存
  info('正在保存...')

  try {
    await api.save(data)
    success('保存成功！')
  } catch (err) {
    error('保存失败，请重试')
  }
}

function validateForm() {
  if (!form.name) {
    warning('请输入姓名')
    return false
  }
  return true
}
</script>
```

---

## 🎯 使用场景对比

### 使用 Modal 的场景

- ✅ 需要用户确认的操作（删除、退出等）
- ✅ 重要信息提示，需要用户明确知晓
- ✅ 需要用户做出选择
- ✅ 表单验证错误（重要的）

### 使用 Toast 的场景

- ✅ 操作成功的反馈
- ✅ 非阻塞的提示信息
- ✅ 进度提示
- ✅ 轻量级的警告
- ✅ 后台任务完成通知

---

## 🎨 视觉效果

### Modal 特点

- 🎯 居中显示
- 🎭 背景遮罩
- 🎪 大图标 + 渐变背景
- 💫 弹跳动画进入
- 🎨 根据类型显示不同颜色
- 🔘 可交互按钮

### Toast 特点

- 📍 右上角显示
- ⚡ 从右侧滑入
- ⏱️ 自动消失
- 📊 进度条显示
- 🎯 可点击关闭
- 🌈 渐变色图标

---

## 📱 响应式设计

### 桌面端

- Modal: 440px 宽度居中
- Toast: 右上角固定，最大宽度 400px

### 移动端

- Modal: 自动适应屏幕，按钮垂直排列
- Toast: 全宽显示，从上方滑入

---

## 🎭 动画效果

### Modal 动画

- **进入**: 缩放 + 旋转 + 弹跳
- **退出**: 缩放 + 旋转
- **图标**: 脉冲 + 旋转弹跳
- **按钮**: 悬停水波纹效果

### Toast 动画

- **进入**: 从右侧滑入 + 缩放
- **退出**: 向右滑出 + 缩放
- **图标**: 弹出动画
- **进度条**: 倒计时收缩

---

## 🎨 主题颜色

### Success (成功)
- 渐变: `#84fab0` → `#8fd3f4`
- 用途: 操作成功、保存完成

### Error (错误)
- 渐变: `#fa709a` → `#fee140`
- 用途: 操作失败、错误提示

### Warning (警告)
- 渐变: `#ffd89b` → `#19547b`
- 用途: 注意事项、警告信息

### Info (信息)
- 渐变: `#a8edea` → `#fed6e3`
- 用途: 一般提示、系统通知

### Confirm (确认)
- 渐变: `#f093fb` → `#f5576c`
- 用途: 需要确认的操作

---

## 💡 最佳实践

### 1. 选择合适的通知类型

```typescript
// ❌ 不好 - 删除操作应该用 Modal 确认
function deleteTask() {
  // 直接删除，没有确认
  api.delete(taskId)
}

// ✅ 好 - 重要操作使用 Modal 确认
async function deleteTask() {
  const confirmed = await showConfirm('确定删除吗？')
  if (confirmed) {
    await api.delete(taskId)
    success('删除成功')
  }
}
```

### 2. 合理使用 Toast 持续时间

```typescript
// ❌ 不好 - 错误信息显示时间太短
error('网络请求失败：超时', 1000)

// ✅ 好 - 错误信息显示更长时间
error('网络请求失败：超时', 4000)

// ✅ 好 - 成功信息可以短一些
success('保存成功', 2000)
```

### 3. 避免通知堆叠

```typescript
// ❌ 不好 - 同时显示多个 Toast
success('任务1完成')
success('任务2完成')
success('任务3完成')

// ✅ 好 - 合并或延迟显示
success('3个任务已完成')
```

### 4. 提供有意义的消息

```typescript
// ❌ 不好 - 消息太简单
error('错误')

// ✅ 好 - 提供具体信息
error('保存失败：文件大小超过限制（最大10MB）')
```

---

## 🔧 高级用法

### 手动关闭 Toast

```typescript
const { remove } = useToast()

// 显示 Toast 并保存 ID
const toastId = info('正在处理...', 0) // duration=0 表示不自动关闭

// 稍后手动关闭
setTimeout(() => {
  remove(toastId)
}, 5000)
```

### 清空所有 Toast

```typescript
const { clear } = useToast()

// 清空所有显示中的 Toast
clear()
```

### 链式调用

```typescript
async function performAction() {
  try {
    await api.action()
    await showSuccess('操作成功')
    await showInfo('3秒后自动跳转')
    await new Promise(r => setTimeout(r, 3000))
    router.push('/')
  } catch (error) {
    await showError('操作失败')
  }
}
```

---

## 📝 迁移指南

### 替换原生 alert/confirm

**之前：**
```typescript
if (confirm('确定要删除吗？')) {
  // 删除
}
```

**现在：**
```typescript
const { showConfirm } = useModal()

const confirmed = await showConfirm('确定要删除吗？')
if (confirmed) {
  // 删除
}
```

**之前：**
```typescript
alert('保存成功！')
```

**现在：**
```typescript
const { showSuccess } = useModal()
// 或
const { success } = useToast()

await showSuccess('保存成功！') // Modal
success('保存成功！') // Toast
```

---

## 🎉 完成！

现在你的应用拥有了：

- ✅ 统一的视觉风格
- ✅ 流畅的动画效果
- ✅ 清晰的用户反馈
- ✅ 响应式设计
- ✅ 易于使用的 API

享受全新的通知系统吧！🚀
