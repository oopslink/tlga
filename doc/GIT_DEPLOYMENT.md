# 🚀 Git 部署和升级方案

## 📋 方案概览

### 方案 A：GitHub Pages（推荐）

**特点：**
- ✅ 自动构建和部署
- ✅ 免费托管
- ✅ 版本控制
- ✅ 一条命令更新
- ✅ 可以回滚
- ⚠️ 首次需联网访问

**适合：** 大多数用户

### 方案 B：Working Copy + iSH（完全离线）

**特点：**
- ✅ 完全离线
- ✅ Git 版本控制
- ✅ 可以查看提交历史
- ✅ 可以回滚版本
- ⚠️ 需要 Working Copy App
- ⚠️ 设置较复杂

**适合：** 需要完全离线的用户

---

## 🎯 方案 A：GitHub Pages 部署

### 步骤 1：初始化 Git 仓库

如果还没有 Git 仓库：

```bash
cd /path/to/tlga

# 初始化仓库
git init

# 创建 .gitignore
cat > .gitignore << 'EOF'
node_modules/
dist/
.DS_Store
*.log
EOF

# 添加所有文件
git add .
git commit -m "Initial commit"
```

### 步骤 2：创建 GitHub 仓库

1. 访问 https://github.com/new
2. 仓库名称：`tlga`
3. 可见性：Private（推荐）或 Public
4. 不要初始化 README、.gitignore 或 LICENSE
5. 点击"Create repository"

### 步骤 3：推送代码到 GitHub

```bash
# 添加远程仓库
git remote add origin https://github.com/你的用户名/tlga.git

# 推送代码
git branch -M main
git push -u origin main
```

### 步骤 4：配置自动部署

#### 方法 1：使用 gh-pages 分支（推荐）

安装部署工具：

```bash
npm install -D gh-pages
```

在 `package.json` 添加部署脚本：

```json
{
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  }
}
```

首次部署：

```bash
npm run deploy
```

#### 方法 2：使用 GitHub Actions（自动化）

创建 `.github/workflows/deploy.yml`：

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          cname: false
```

提交并推送：

```bash
git add .github/workflows/deploy.yml
git commit -m "Add GitHub Actions deploy workflow"
git push
```

### 步骤 5：启用 GitHub Pages

1. 访问仓库的 Settings → Pages
2. Source 选择 `gh-pages` 分支
3. 点击 Save

**几分钟后，应用会部署到：**
```
https://你的用户名.github.io/tlga/
```

### 步骤 6：在 iPad 上安装

1. **首次访问**（需联网）
   - Safari 打开：`https://你的用户名.github.io/tlga/`
   - 等待完全加载（PWA 会自动缓存）

2. **添加到主屏幕**
   - 点击分享 → 添加到主屏幕
   - 命名为"小学霸"

3. **测试离线**
   - 开启飞行模式
   - 打开 App
   - 完全正常使用！

✅ **完成！** 现在应用已部署，所有代码和数据都在 iPad 本地。

---

## 🔄 更新流程（方案 A）

### 标准更新（推荐）

```bash
# 1. 修改代码...

# 2. 提交更改
git add .
git commit -m "✨ 新增任务管理功能"

# 3. 推送到 GitHub
git push

# 4. 部署（方法 1：手动）
npm run deploy

# 或（方法 2：GitHub Actions 自动部署）
# 推送后自动触发，无需手动操作
```

### iPad 上更新

**自动更新（PWA）：**
1. 联网后打开 App
2. PWA 自动检测新版本
3. 后台下载更新
4. 重启 App 应用更新

**手动更新：**
1. Safari 访问 `https://你的用户名.github.io/tlga/`
2. 强制刷新（长按刷新按钮）
3. 重新添加到主屏幕

**数据保留：**
- ✅ 所有用户数据自动保留
- ✅ 更新不影响 localStorage

---

## 🎯 方案 B：Working Copy + iSH（完全离线）

### 概述

```
电脑                iPad Working Copy          iPad iSH
┌────────┐         ┌──────────────┐           ┌─────────┐
│ Git    │  Push   │ Git          │   Export  │ 运行    │
│ Repo   │ ──────→ │ Clone/Pull   │ ────────→ │ 服务器  │
│        │         │              │           │         │
└────────┘         └──────────────┘           └─────────┘
                          ↓                          ↓
                     版本控制                  localhost:8080
```

### 步骤 1：准备 Git 仓库

同方案 A 的步骤 1-3，将代码推送到 GitHub。

### 步骤 2：在 iPad 上安装 Working Copy

1. App Store 搜索 "**Working Copy**"
2. 安装（免费版足够使用）

### 步骤 3：Clone 仓库到 iPad

在 Working Copy 中：

1. 点击 "+" → Clone repository
2. 输入仓库 URL：`https://github.com/你的用户名/tlga.git`
3. 如果是私有仓库，需要输入 GitHub token
4. 等待克隆完成

### 步骤 4：首次部署

#### 4.1 构建应用（在电脑上）

```bash
# 构建生产版本
npm run build

# 提交构建结果
git add dist/
git commit -m "Build production"
git push
```

#### 4.2 在 iPad 导出到 iSH

1. **Working Copy 中拉取最新代码**
   - 打开仓库
   - 下拉刷新

2. **导出 dist 文件夹**
   - 找到 `dist` 文件夹
   - 点击共享图标
   - 选择"导出"
   - 保存到 Files App

3. **在 iSH 中设置**
```bash
# 在 iSH 中
cd ~
mkdir -p tlga

# 从 Files App 复制 dist 内容到 ~/tlga

# 启动服务器
cd ~/tlga
python3 -m http.server 8080
```

4. **Safari 访问和安装**
   - 访问 `http://localhost:8080`
   - 添加到主屏幕

### 步骤 5：更新流程（方案 B）

#### 5.1 在电脑上更新

```bash
# 1. 修改代码
# ...

# 2. 提交
git add .
git commit -m "✨ 新功能"

# 3. 构建
npm run build

# 4. 提交构建结果
git add dist/
git commit -m "Build v1.1.0"

# 5. 推送
git push
```

#### 5.2 在 iPad 上更新

1. **Working Copy 中拉取**
   - 打开仓库
   - 下拉刷新（Pull）
   - 查看新提交

2. **导出新版本**
   - 导出 `dist` 文件夹
   - 保存到 Files App

3. **iSH 中替换**
```bash
# 备份旧版本
mv ~/tlga ~/tlga-backup-$(date +%Y%m%d)

# 创建新目录
mkdir ~/tlga

# 从 Files App 复制新版本文件

# 重启服务器
cd ~/tlga
python3 -m http.server 8080
```

4. **验证更新**
   - Safari 访问 `http://localhost:8080`
   - 清除缓存
   - 重新添加到主屏幕

---

## 🔧 自动化脚本

### 电脑端：自动构建和推送

创建 `scripts/deploy.sh`：

```bash
#!/bin/bash

set -e

echo "🔨 构建应用..."
npm run build

echo "📝 提交更改..."
git add .

# 询问提交信息
echo "请输入提交信息："
read COMMIT_MSG

if [ -z "$COMMIT_MSG" ]; then
    COMMIT_MSG="Update $(date +%Y-%m-%d)"
fi

git commit -m "$COMMIT_MSG"

echo "📋 提交构建结果..."
git add dist/ -f
git commit -m "Build: $COMMIT_MSG"

echo "🚀 推送到远程..."
git push

echo "✅ 部署完成！"
echo ""
echo "GitHub Pages: https://你的用户名.github.io/tlga/"
echo "或在 iPad Working Copy 中拉取更新"
```

使用：
```bash
chmod +x scripts/deploy.sh
./scripts/deploy.sh
```

### iPad 端：快速更新（快捷指令）

创建 iOS 快捷指令：

1. 打开"快捷指令"App
2. 创建新快捷指令
3. 添加动作：
   - 打开 App → Working Copy
   - 等待 5 秒
   - 运行 URL Scheme：`working-copy://pull?repo=tlga`
   - 显示通知："代码已更新"
4. 命名为"更新小学霸"
5. 添加到主屏幕

**使用：**
- 点击快捷指令图标
- 自动打开 Working Copy 并拉取更新
- 然后手动导出到 iSH

---

## 📊 两种方案对比

| 特性 | GitHub Pages | Working Copy + iSH |
|------|--------------|-------------------|
| **部署复杂度** | 简单 ⭐⭐⭐⭐⭐ | 中等 ⭐⭐⭐ |
| **更新复杂度** | 简单 ⭐⭐⭐⭐⭐ | 中等 ⭐⭐⭐ |
| **首次联网** | 需要 | 需要（克隆代码） |
| **更新联网** | 需要 | 需要（拉取代码） |
| **完全离线** | ❌ | ✅ |
| **自动更新** | ✅ PWA 自动 | ❌ 手动 |
| **版本控制** | ✅ | ✅ |
| **回滚能力** | ✅ | ✅ |
| **成本** | 免费 | 免费 |

---

## 🔍 版本管理

### 使用 Git Tags 管理版本

```bash
# 创建版本标签
git tag -a v1.0.0 -m "首次发布"
git push origin v1.0.0

# 列出所有版本
git tag -l

# 回滚到特定版本
git checkout v1.0.0
npm run build
npm run deploy
```

### 语义化版本号

遵循 [Semantic Versioning](https://semver.org/)：

- **v1.0.0** - 主版本.次版本.修订版本
- **主版本**：不兼容的 API 修改
- **次版本**：向下兼容的功能新增
- **修订版本**：向下兼容的问题修正

示例：
```bash
# 新功能
git tag v1.1.0 -m "新增任务管理"

# Bug 修复
git tag v1.1.1 -m "修复审批页面错误"

# 重大更新
git tag v2.0.0 -m "全新界面设计"
```

### 自动版本号更新

在 `package.json` 中维护版本号：

```json
{
  "name": "tlgapp",
  "version": "1.0.0"
}
```

更新版本：
```bash
# 自动增加版本号
npm version patch  # 1.0.0 → 1.0.1
npm version minor  # 1.0.1 → 1.1.0
npm version major  # 1.1.0 → 2.0.0

# 自动创建 git tag 和 commit
```

---

## 🔄 持续集成工作流

### 开发和发布流程

```
开发分支                主分支               GitHub Pages
┌──────────┐          ┌────────┐          ┌──────────┐
│ develop  │  合并    │ main   │  自动部署 │ 生产环境 │
│          │ ──────→  │        │ ──────→  │          │
│ 日常开发 │          │ 稳定版 │          │ 用户访问 │
└──────────┘          └────────┘          └──────────┘
```

**工作流程：**

```bash
# 1. 日常开发在 develop 分支
git checkout -b develop
# ... 开发新功能
git add .
git commit -m "WIP: 开发中"
git push origin develop

# 2. 功能完成后合并到 main
git checkout main
git merge develop
git push

# 3. 自动触发部署（GitHub Actions）
# 或手动部署
npm run deploy

# 4. 创建版本标签
git tag v1.1.0
git push origin v1.1.0
```

---

## 🆘 故障排查

### 问题 1：GitHub Pages 没有更新

**检查：**
```bash
# 1. 确认代码已推送
git log --oneline -5

# 2. 检查 gh-pages 分支
git checkout gh-pages
git log --oneline -5

# 3. 查看 GitHub Actions 状态
# 访问：https://github.com/你的用户名/tlga/actions
```

**解决：**
```bash
# 强制重新部署
npm run deploy -- --force
```

### 问题 2：Working Copy 拉取失败

**检查：**
- 网络连接
- GitHub token 是否有效
- 仓库是否是私有的

**解决：**
- 重新生成 GitHub token
- 在 Working Copy 中重新认证

### 问题 3：iPad 上看到旧版本

**原因：** PWA/浏览器缓存

**解决：**
```bash
1. 清除 Safari 缓存
2. 强制刷新页面
3. 卸载并重新添加到主屏幕
```

### 问题 4：dist 文件夹被 .gitignore 忽略

**检查：**
```bash
# 查看 .gitignore
cat .gitignore
```

**解决方案 A：** 使用 gh-pages 工具（推荐）
```bash
# gh-pages 会自动处理，不需要提交 dist
npm run deploy
```

**解决方案 B：** 强制添加 dist
```bash
git add dist/ -f
git commit -m "Add build files"
```

---

## 🎯 最佳实践

### 1. 分支策略

```
main          稳定版本，只接受 PR
develop       开发分支，日常开发
feature/*     功能分支
bugfix/*      修复分支
release/*     发布分支
```

### 2. 提交信息规范

使用 [Conventional Commits](https://www.conventionalcommits.org/)：

```bash
feat: 新增任务管理功能
fix: 修复审批页面渲染错误
docs: 更新部署文档
style: 优化按钮样式
refactor: 重构数据存储逻辑
test: 添加单元测试
chore: 更新依赖包
```

### 3. 版本发布检查清单

- [ ] 所有测试通过
- [ ] 更新 CHANGELOG.md
- [ ] 更新版本号（package.json）
- [ ] 创建 git tag
- [ ] 部署到生产环境
- [ ] 验证生产环境正常
- [ ] 通知用户更新

---

## 📝 CHANGELOG 模板

创建 `CHANGELOG.md`：

```markdown
# Changelog

## [Unreleased]

### Added
- 正在开发的新功能

## [1.1.0] - 2026-02-15

### Added
- ✨ 新增任务管理功能
- ✨ 新增自定义任务定义

### Changed
- 🎨 优化设置页面布局
- 🎨 改进移动端适配

### Fixed
- 🐛 修复审批页面渲染错误
- 🐛 修复 PWA meta 标签警告

## [1.0.0] - 2026-02-10

### Added
- 🎉 首次发布
- ✨ 周计划管理
- ✨ 每日进度填写
- ✨ 审批系统
- ✨ 历史记录查看
```

---

## 🎉 总结

### 推荐：GitHub Pages 方案

**一次配置，终身受益：**

```bash
# 1. 初始设置（一次性）
git init
git remote add origin https://github.com/你的用户名/tlga.git
git push -u origin main
npm install -D gh-pages

# 2. 日常使用（每次更新）
git add .
git commit -m "✨ 新功能"
git push
npm run deploy

# 3. iPad 自动更新
# PWA 自动检测并更新，无需操作
```

**优势总结：**
- ✅ 最简单的更新流程
- ✅ 自动化部署
- ✅ PWA 自动更新
- ✅ 完整的版本控制
- ✅ 可以轻松回滚
- ✅ 免费托管

**现在就开始吧！** 🚀

---

需要帮助设置 GitHub Actions 或其他自动化配置吗？
